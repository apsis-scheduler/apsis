params: [exp]

program:
  type: procstar
  group_id: default
  argv:
    - "/usr/bin/python"
    - "-c"
    - |
      import sys
      n = 10 ** int(sys.argv[1])
      for _ in range(n):
        pass
    - "{{ exp }}"

